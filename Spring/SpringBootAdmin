Have to add below line in application.properties at Client side, otherwise, no info shown in Applicaion Detail page
	management.security.enabled=false

Ignore redis health check by adding below config:
	management.health.redis.enabled=false

Need add below config if running Spring Boot Admin in Cantainer like Tomcat:
	spring.boot.admin.client.service-url=http://

